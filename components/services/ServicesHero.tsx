import { PageHero } from "@/components/shared/PageHero";

export function ServicesHero() {
  return (
    <PageHero
      eyebrow="Services"
      plainLead="Strategy first."
      accentPhrase="Then everything else."
      accentDot
      lede="Most agencies sell you a deliverable. We sell you the argument first, then build every asset to serve it — in-house, from positioning to broadcast delivery."
    />
  );
}
