import { PageHero } from "@/components/shared/PageHero";

export function ContactHero() {
  return (
    <PageHero
      eyebrow="Contact"
      plainLead="One conversation."
      accentPhrase="No deck required."
      accentDot
      lede="Tell us what you’re trying to move, and we’ll tell you honestly whether we’re the right agency for it — no discovery workshop, just a conversation."
    />
  );
}
