import { PageHero } from "@/components/shared/PageHero";

export function StudioHero() {
  return (
    <PageHero
      eyebrow="Studio"
      plainLead="Senior people started this."
      accentPhrase="They still run it."
      accentDot
      lede="AdPro 360 was built by people who’d already spent a decade inside the industries they now serve — banking, broadcast, film — not by an agency that discovered strategy afterward."
    />
  );
}
